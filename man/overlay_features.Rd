% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/overlay.R
\name{overlay_features}
\alias{overlay_features}
\title{Overlay Features with Watersheds}
\usage{
overlay_features(features, watersheds, join = "intersects")
}
\arguments{
\item{features}{An \code{sf} data frame of features (typically points) to
overlay.}

\item{watersheds}{An \code{sf} data frame of watershed polygons.}

\item{join}{Character. Spatial predicate: \code{"intersects"} (default),
\code{"within"}, or \code{"nearest"}. \code{"nearest"} assigns each feature to the
closest watershed regardless of containment.}
}
\value{
An \code{sf} data frame with the original \code{features} geometry and
both feature and matched watershed attributes. Features that do not
fall within any watershed will have \code{NA} for watershed columns
(except when \code{join = "nearest"}). Duplicate column names from
\code{watersheds} are disambiguated with a \code{.ws} suffix.
}
\description{
Performs a spatial join to determine which watershed polygon each feature
falls within. This is useful for attributing monitoring sites, permits,
or other point data to their containing watershed or HUC boundary.
}
\details{
When column names collide between \code{features} and \code{watersheds}, the
watershed columns receive a \code{.ws} suffix (e.g. \code{name} becomes \code{name.ws}).
}
\examples{
\donttest{
# Load sample Elkhorn Creek data
sites <- sf::st_read(
  system.file("extdata", "elkhorn_sites.geojson", package = "watershedtools"),
  quiet = TRUE
)
basin <- sf::st_read(
  system.file("extdata", "elkhorn_basin.geojson", package = "watershedtools"),
  quiet = TRUE
)

# Which sites fall inside the basin?
result <- overlay_features(sites, basin)
print(result[, c("site_id", "huc8")])
}
}
