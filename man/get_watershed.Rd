% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boundary.R
\name{get_watershed}
\alias{get_watershed}
\title{Get Watershed Boundary}
\usage{
get_watershed(point = NULL, comid = NULL)
}
\arguments{
\item{point}{An \code{sf} or \code{sfc} point object (single feature) to use as
the watershed outlet. Automatically transformed to WGS 84 if needed.
Supply either \code{point} or \code{comid}, not both.}

\item{comid}{Integer. An NHDPlus COMID identifying the flowline whose
drainage basin to retrieve. Supply either \code{comid} or \code{point}, not both.}
}
\value{
An \code{sf} polygon (or multipolygon) object representing the
upstream drainage basin in WGS 84 (EPSG:4326).
}
\description{
Retrieves the drainage basin boundary for a given NHDPlus COMID or
geographic point using the NLDI (Network Linked Data Index) web service.
Requires an internet connection and the \pkg{nhdplusTools} package.
}
\details{
When a \code{point} is supplied the nearest NHDPlus flowline is identified
automatically with \code{\link[nhdplusTools:discover_nhdplus_id]{nhdplusTools::discover_nhdplus_id()}}, and its
drainage basin is returned.
}
\examples{
\dontshow{if (requireNamespace("nhdplusTools", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\donttest{
# Get watershed for a COMID on Elkhorn Creek, KY
basin <- get_watershed(comid = 9734460)
plot(sf::st_geometry(basin))

# Get watershed from a point (Frankfort, KY)
pt <- sf::st_sfc(sf::st_point(c(-84.87, 38.20)), crs = 4326)
basin2 <- get_watershed(point = pt)
}
\dontshow{\}) # examplesIf}
}
